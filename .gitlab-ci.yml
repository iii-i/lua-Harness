image: python:alpine

before_script:
  - apk --update add git
  - pip3 install --root-user-action=ignore mkdocs

pages:
  script:
  - mkdocs build -d public
  - git reset --hard
  artifacts:
    paths:
    - public
  only:
  - master
